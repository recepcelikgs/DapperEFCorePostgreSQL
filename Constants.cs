namespace DapperEFCorePostgreSQL
{
    public static class Constants
    {
        public static readonly string ConnectionString = "Host=localhost;Database=postgres;Username=user;Password=pass";
    }
}
